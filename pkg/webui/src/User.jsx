export default class User {
    constructor(id, odeName, firstName, lastName, email) {
      this.id = id;
      this.odeName = odeName;
      this.firstName = firstName;
      this.lastName = lastName;
      this.email = email;
    }
  }